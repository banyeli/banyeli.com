import Link from "next/link";
import { demoLeads, scoreBand } from "@/lib/real-estate";
import "../real-estate.css";
export default function LeadsPage() { return <main className="real-estate-page"><nav className="re-nav"><Link href="/dashboard">Banyeli</Link><Link href="/real-estate">Real Estate</Link><a href="/real-estate/follow-ups">Follow-ups</a></nav><header className="re-header"><div><p className="eyebrow">Private CRM · Demo workspace</p><h1>Your leads.</h1><p>Every score is explained. No communication is sent from this page.</p></div></header><section className="re-panel"><div className="re-panel-heading"><div><p className="eyebrow">All leads</p><h2>Private lead records</h2></div><span className="re-demo">Demo data</span></div><div className="re-leads">{demoLeads.map((lead)=><article className="re-lead" key={lead.id}><div><p className="re-lead-name">{lead.name}</p><p>{lead.type} · {lead.city} · {lead.stage.replaceAll("_", " ")}</p></div><div className="re-score"><b>{lead.score}</b><span>{scoreBand(lead.score)}</span></div><p className="re-reason">{lead.lastActivity}</p><p className="re-next">Why: {lead.scoreReasons.join(" · ")}</p></article>)}</div></section></main>; }
